on:
  workflow_dispatch:
    inputs:
      message:
        description: '表示するメッセージ'
        required: false
        default: 'hello'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "${{ github.event.inputs.message }}"
